ollama_url: "http://localhost:11434"
listen_addr: ":9400"
poll_interval: "15s"

proxy:
  enabled: true
  listen_addr: ":9401"

gpu:
  enabled: true
  # poll_interval: "5s"      # separate GPU poll interval; defaults to poll_interval above
  # sysfs_base: /sys/class/drm  # override for testing; default is /sys/class/drm

log_level: "info"
