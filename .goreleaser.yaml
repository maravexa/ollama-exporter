version: 2

project_name: ollama-exporter

before:
  hooks:
    - go mod tidy

builds:
  - id: ollama-exporter
    main: ./cmd/exporter
    binary: ollama-exporter
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.Version={{.Version}}

nfpms:
  - id: ollama-exporter
    package_name: ollama-exporter
    ids:
      - ollama-exporter
    formats:
      - deb
      - rpm
    maintainer: maravexa
    description: Prometheus exporter for Ollama LLM inference
    license: MIT
    bindir: /usr/bin
    contents:
      - src: ollama-exporter.yml
        dst: /etc/ollama-exporter/ollama-exporter.yml
        type: config
      - src: packaging/ollama-exporter.service
        dst: /usr/lib/systemd/system/ollama-exporter.service
    scripts:
      postinstall: packaging/postinstall.sh
      preremove: packaging/preremove.sh
    dependencies:
      - systemd

archives:
  - id: ollama-exporter
    ids:
      - ollama-exporter
    formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    files:
      - README.md
      - LICENSE
      - ollama-exporter.yml.example

checksum:
  name_template: "checksums.txt"

# nfpm artifacts (.deb, .rpm) are registered automatically; no extra_files needed.
# Listing them here caused "artifact already present" 422 errors on GitHub release upload.
release: {}

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

# TODO: migrate dockers/docker_manifests to dockers_v2 syntax when targeting
# GoReleaser ≥ v2.x fully-deprecated release (not causing current failures).
dockers:
  - id: amd64
    image_templates:
      - "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}-amd64"
    dockerfile: Dockerfile.release
    use: buildx
    goarch: amd64
    build_flag_templates:
      - "--platform=linux/amd64"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source=https://github.com/maravexa/ollama-exporter"
  - id: arm64
    image_templates:
      - "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}-arm64"
    dockerfile: Dockerfile.release
    use: buildx
    goarch: arm64
    build_flag_templates:
      - "--platform=linux/arm64"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source=https://github.com/maravexa/ollama-exporter"

docker_manifests:
  - name_template: "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}"
    image_templates:
      - "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}-amd64"
      - "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}-arm64"
  - name_template: "ghcr.io/maravexa/ollama-exporter:latest"
    image_templates:
      - "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}-amd64"
      - "ghcr.io/maravexa/ollama-exporter:{{ .Tag }}-arm64"
